package com.ramichanstore.backend.modules.shipments.entity;

/** Vía de envío del embarque desde Japón: correo (EMS), aéreo (AVIA) o marítimo (BARCO). */
public enum ShipmentType {
    EMS, AVIA, BARCO
}
