export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api',
  /** Origen del backend (sin /api) para armar URLs de recursos servidos directamente, como imágenes. */
  serverOrigin: 'http://localhost:8080',
};
